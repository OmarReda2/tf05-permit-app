Fix Slice 12 print PDF filename issue only.

Current issue:
When clicking Print / Save PDF, the browser print dialog default PDF filename uses the browser tab title/app name, not the permit number.

Required behavior:
- When printing a permit, the default print/PDF filename should use the permit number.
- Example filename/title:
  PTW-20260509-153022
  or
  TF05-PTW-20260509-153022
- After printing, restore the original document title.
- Keep the fix minimal.

Technical guidance:
- Before calling window.print(), temporarily set document.title to the permit number.
- After print is triggered, restore the previous document.title.
- Use permit.permitNumber if available.
- Fallback to "TF05-Permit" if permitNumber is missing.
- Avoid external PDF libraries.
- Do not implement server-side PDF.
- Do not add dependencies.
- Do not refactor unrelated permit details logic.

Example logic:
const previousTitle = document.title;
document.title = permit.permitNumber || 'TF05-Permit';
window.print();
setTimeout(() => document.title = previousTitle, 500);

If using afterprint event is cleaner in the current code, use it.

After fixing:
- Explain changed files.
- Explain why the fix is minimal.
- Run npm run build.
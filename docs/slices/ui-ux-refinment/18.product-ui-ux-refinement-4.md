Implement Slice 18 only: Final UI Interaction Polish.

Context:
The app UI is mostly acceptable now, but there are a few final interaction and branding issues that need to be fixed carefully for both desktop and mobile.

Important:
This is a UI/UX polish slice only.
Do not change business logic.
Do not change Firestore models.
Do not change Firestore rules.
Do not change auth/permit workflow.
Do not add dependencies.
Do not rewrite the app.
Keep existing functionality working.

1. Smooth popup/menu transitions
- Any popup, dropdown, menu, dialog, or confirmation UI should appear and disappear smoothly.
- Do not show popups suddenly.
- Use lightweight CSS transitions:
  - opacity
  - transform
  - scale or translateY
- Keep motion subtle and professional.
- Respect accessibility: popup should still be usable by keyboard.
- Do not add animation libraries.

Apply to:
- account dropdown
- confirmation dialogs if any
- action feedback popups/snackbars if any
- theme menu/toggle if using a small menu

2. Dark/light mode control refinement
- Current dark mode button is too plain.
- Replace it with a more professional theme control.
- Use an icon-based toggle or compact segmented control.
- It should be clearly visible but not crowded.
- It should not be hidden inside the account dropdown.
- It should work on login page and inside the authenticated app.
- It should align with app colors in both light and dark mode.
- Persist theme in localStorage.
- Avoid a large “Use dark mode” text button.
- Suggested labels/icons:
  - sun icon for light
  - moon icon for dark
  - or one compact icon button with tooltip/aria-label

Desktop:
- Place theme control in the top header near the account area, but separated enough to avoid crowding.

Mobile:
- Place theme control in a clean accessible place:
  - login page header/card area
  - authenticated top header/account area
  - do not crowd bottom navigation

3. Sidebar collapse position like Firebase
- Move the sidebar collapse/expand control to the bottom of the sidebar/rail, similar to Firebase.
- It should not appear beside the logo anymore.
- It should not go under the logo.
- In expanded mode, show a small bottom collapse button/icon.
- In collapsed mode, show a bottom expand button/icon.
- The button should be fixed at the bottom of the sidebar area.
- The sidebar should remain sticky/fixed during page scroll.
- Collapse/expand should be smooth.
- Collapsed rail should keep icons visible and aligned.

Desktop only:
- Apply this sidebar collapse behavior only on desktop/tablet sidebar layout.

Mobile:
- Do not show this sidebar collapse button.
- Keep mobile bottom navigation behavior.

4. Sidebar logo branding
- The browser tab favicon is now acceptable.
- Use the same visual logo style in the sidebar logo area.
- Replace the current sidebar blue “TF” square if needed so it matches the favicon/tab logo style.
- Keep the app name beside it in expanded sidebar.
- In collapsed sidebar, show only the logo clearly.
- Logo should look crisp in light and dark mode.
- Do not use Angular logo anywhere.

5. Account dropdown mobile spacing
- Review account dropdown in mobile view.
- Ensure it has proper margin from screen edges.
- It should not overflow horizontally.
- It should not feel cramped.
- Dropdown should align under the account control where possible.
- If viewport is narrow, align it safely to the right with max-width.
- Keep touch targets comfortable.
- Keep outside-click close behavior working.
- Keep Escape close behavior working.

6. Verification
After implementation, verify:
- Sidebar remains fixed/sticky while scrolling on desktop.
- Sidebar collapse/expand control is at the bottom like Firebase.
- Collapsed sidebar has a clear expand button at the bottom.
- Sidebar logo matches the favicon/tab logo style.
- No Angular favicon/logo remains.
- Theme toggle is professional, icon-based, and visible.
- Theme toggle works on login page.
- Theme toggle works in authenticated app.
- Theme persists after refresh.
- Account dropdown has smooth open/close transition.
- Account dropdown spacing works on mobile.
- Any popup/menu/dialog appears smoothly.
- Mobile bottom navigation is not broken.
- No business logic changed.
- No dependencies added.

Before changing files:
- Briefly explain planned changes.
- List likely files to change.

After changing files:
- Summarize exactly what changed.
- Confirm no business logic, Firestore model, or rules changes.
- Confirm no dependencies were added.
- Run npm run build.
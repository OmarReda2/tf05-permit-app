Implement Slice 19 only: Logo Consistency, Login Background, and Theme Toggle Polish.

Context:
The app is almost acceptable, but three visual issues remain:
1. The sidebar/app logo looks messed up and does not match the browser tab favicon.
2. The login page background grid/squares is a good direction, but it is too light/subtle and should be slightly more visible while staying professional.
3. The dark mode toggle is well constructed, but the moon icon has an unwanted dark circle in the middle. Remove/fix that visual issue.

Important:
This is a UI/UX polish slice only.
Do not change business logic.
Do not change Firestore models.
Do not change Firestore rules.
Do not change auth/permit workflow.
Do not add dependencies.
Do not rewrite the app.

1. Logo consistency
- The browser tab favicon/icon currently looks acceptable.
- Use the same visual logo style for the sidebar/logo area and login brand area.
- Do not use the broken/misaligned current app icon.
- Do not use the Angular logo.
- The logo should look crisp in:
  - browser tab
  - sidebar expanded
  - sidebar collapsed
  - login page
  - dark mode
  - light mode
- Keep the logo simple and aligned with the existing favicon style.
- If the favicon is an SVG or asset file, reuse it where possible instead of recreating a different logo.
- The sidebar logo should not be stretched, cropped, or visually distorted.
- In expanded sidebar: show logo + app name.
- In collapsed sidebar: show logo only, centered and clean.

2. Login background refinement
- The login page grid/square background direction is good.
- Make the background pattern slightly more visible.
- Keep it subtle, professional, and eye-comfortable.
- Do not make it noisy or distracting.
- It should support both light and dark mode.
- Dark mode can have slightly stronger but still soft grid visibility.
- Light mode can use soft blue/neutral lines with low opacity.
- Keep the login card readable and dominant.
- Do not turn the login page into a marketing/landing page.

3. Theme toggle moon icon refinement
- The dark mode toggle is generally good, but the moon icon currently has an unwanted dark circle in the middle.
- Remove/fix the dark inner circle.
- The moon icon should look clean, simple, and intentional.
- It should remain visually clear in both light and dark mode.
- Keep the toggle professional and aligned with the current app style.
- Do not replace the whole theme system unless necessary.
- Do not add icon libraries or dependencies.
- Prefer fixing the existing CSS/SVG/icon implementation.

4. Visual constraints
- Maintain the Google/Firebase-console-inspired style.
- Use existing CSS variables/design tokens if available.
- Keep colors calm.
- Avoid heavy gradients.
- Avoid strong decorative graphics.
- Avoid animation changes unless necessary.

5. Verification
After implementation, verify:
- Browser tab icon is still correct.
- Sidebar logo matches the tab/favicon logo style.
- Login logo matches the same style.
- Logo is not distorted in collapsed sidebar.
- Login background grid is more visible but still subtle.
- Moon icon no longer has the unwanted dark circle.
- Theme toggle still works correctly.
- Light mode looks clean.
- Dark mode looks clean.
- Mobile login still looks good.
- No business logic changed.
- No dependencies added.

Before changing files:
- Briefly explain planned changes and files likely to change.

After changing files:
- Summarize exactly what changed.
- Confirm no business logic, Firestore model, or rules changes.
- Confirm no dependencies were added.
- Run npm run build.
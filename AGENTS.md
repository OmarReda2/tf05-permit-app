## Current Project

Project name: TF-05 Safety Permit App

Target:
Build a minimal responsive Angular + Firebase MVP for construction Permit-to-Work management.

Current stack:
- Angular
- SCSS
- Firebase SDK
- Firebase Auth later
- Firestore later
- Firebase Hosting later

Important:
- Do not use @angular/fire for now because of Angular version conflict.
- Use Firebase SDK directly when Firebase integration starts.
- Build slice by slice.
- Do not implement the whole app at once.